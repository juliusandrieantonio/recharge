export interface SystemSettings {
  points_per_bottle: number;
  minutes_per_points: number;
  bottles_per_school_chair: number;
  bottles_per_kg_waste: number;
  bottles_per_trash_bag: number;
  energy_save_per_bottle: number;
}